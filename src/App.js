// import data from './data';

function App() {
  return (
    <div>
      <header><a href="/">Amazona</a></header>
      <main>
      <h1>Feuture Products</h1>
      {/* {
        data.products.map((prod)=>(
          <div key={prod.slug}>
            <img src="{prod.image}" alt="{prod.name}" />
            <p>{prod.name}</p>
            <p>{prod.price}</p>
          </div>
        ))
      } */}
      
      </main>  
    </div>
  );
}

export default App;
